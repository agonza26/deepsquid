outline usage (for fish)

set up 2 materials, one for the normal material, one for the highlight material
if the material is the child, find the child
		/*
		foreach(Transform child in this.transform)
		{
			child.gameObject.GetComponentInChildren<Renderer>().material = HLMat;
		}
	
		foreach(Transform child in this.transform)
		{
			child.gameObject.GetComponentInChildren<Renderer>().material = normalMat;
		}
		*/
