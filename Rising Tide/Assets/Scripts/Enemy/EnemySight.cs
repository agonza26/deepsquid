using UnityEngine;
using System.Collections;

public class EnemySight : MonoBehaviour {
	/*
	public float fieldOfViewAngle = 110f;
	public bool playerInSight;
	public Vector3 personalLastSighting;



	//reference to navmesh for
	private NavMeshAgent nav;
	private SphereCollider col;
	private	Animator anim;
	private LastPlayerSighting lastPlayerSighting;  //might need to work on this
	private GameObject player;
	//private Animator playerAnim; //for hearing
	//private PlayerHealth playerhealth;
	//private HashIDS hash;
	private Vector3 previousSighting;


	// Use this for initialization
	void Start () {
		nav = GetComponent<NavMeshAgent> ();
		col = GetComponent<SphereCollider> ();
		anim = GetComponent<Animator> ();
		lastPlayerSighting = GameObject.FindGameObjectWithTag(Tags.gameController).GetComponent<LastPlayerSighting> ();
		player = GameObject.FindGameObjectsWithTag(Tags.player);
		//playerAnim = player.GetComponent<Animator>();
		//playerHealth = player.GetComponent<PlayerHealth>();
		//Hash128 = GameObject.FindGameObjectWithTag<Tags.gameController).GetComponent<HashIDs>();
	
	}
	
	// Update is called once per frame
	void Update () {
		if (lastPlayerSighting.position != previousSighting)
			personalLastSighting = lastPlayerSighting.position;

		previousSighting = lastPlayerSighting.position;



	}




	*/

}
